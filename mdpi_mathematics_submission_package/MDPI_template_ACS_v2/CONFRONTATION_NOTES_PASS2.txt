===============================================================
CONFRONTATION NOTES — PASS 2 (Post-fix re-read + spot-checks)
Date: 2026-05-17
Based on: main_en.tex after Confrontation Pass 1 (4 fixes applied)
===============================================================

=== FIXES APPLIED IN PASS 1 (confirmed) ===

[CF1-FIX] §4.5 Residual injection — APPLIED ✓
  - Equation changed from W_sum = W_res + W_addon to W_res only
  - Text updated: W_addon explained as E3-only, inert in standard mode
  - "The two projections...both injection paths" → corrected to single path
  - q_meta² attenuation description preserved and still correct ✓

[CF1-FIX continued] Figure 1 caption — APPLIED ✓
  - "Two parallel zero-initialised projections (W_res, W_addon)...
    via residual injection" → corrected to "One zero-initialised
    residual projection (W_res)... W_addon for E3 only"

[CF1-FIX continued] §4.8 Parameter budget — APPLIED ✓
  - "two metadata-to-ECG residual projections (W_res and W_addon)"
    → updated to describe their respective roles (standard vs E3)

[CF2-FIX] §3.4 Pos-weights formula — APPLIED ✓
  - Old formula (normalized to NORM) removed
  - New formula: w_c^+ = clip(n_neg,c / n_pos,c, 0.5, 30.0) ✓
  - "held fixed across all runs" → "computed dynamically per run" ✓
  - Intro sentence updated to define n_pos,c and n_neg,c ✓

=== ADDITIONAL FIX FOUND IN PASS 2 ===

[CF5] Table tab:dataset: NORM positive weight still showed 1.00

  MANUSCRIPT (Table 1, tab:dataset, before fix):
    "Positive weight w_c^+" row: 1.00 | 2.98 | 3.16 | 3.46 | 7.22

  ACTUAL TRAINING LOG:
    pos_weights = [1.2930, 2.9776, 3.1610, 3.4582, 7.2199]
    → NORM = 1.29 (not 1.00)

  FIX APPLIED:
    Table row: 1.00 → 1.29 for NORM ✓
    (MI, STTC, CD, HYP values 2.98, 3.16, 3.46, 7.22 match logs ✓)

  LaTeX recompiled: 26 pages, 0 errors ✓

=== SPOT-CHECKS PERFORMED IN PASS 2 ===

[S1] §3.4 eq:posweights label still exists and is referenced correctly
  in §5.1 "BCE_{w+} ...class weights from Eq. posweights" ✓
  in tab:dataset caption "derived via Eq. posweights" ✓

[S2] eq:hts label still present after equation-type change
  (align → equation); referenced correctly in Q3 discussion
  "zero-initialised residual injection (Eq. hts)" ✓

[S3] No remaining occurrences of:
  - "W_sum" or "W_{\mathrm{sum}}" ✓
  - "normalising to NORM" or "normalizing to NORM" ✓
  - "1.0" as NORM pos_weight (Table 1 now shows 1.29) ✓
  - "held fixed across all runs" for pos_weights ✓
  - "two projections" implying both active in standard mode ✓

[S4] Loss formula in §5.1 verified:
  - 0.52 BCE_{w+}(fused) + 0.30 BCE_{w+}(ecg) + 0.10 q_meta BCE(meta) + 0.08 L_AUC
  - Code: fused_w=0.52, ecg_w=0.30, meta_w=0.10, lauc_w=0.08 ✓
  - Meta BCE unweighted (no pos_weight): code uses
    F.binary_cross_entropy_with_logits without pos_weight ✓
  - Fused/ECG use pos-weighted BCE: code uses criterion (with pos_weight) ✓
  - Effective meta term = 0.10 × q_meta × BCE ✓ (meta_quality_w applied inside)

[S5] Three heads verified:
  - ℓ_ecg computed on h_ts1 BEFORE residual injection ✓
  - logits_fused = head_fused(z) + 0.05 × meta_quality × logits_meta ✓

[S6] GLU gate: h ⊙ σ(Linear_2(ReLU(Linear_1(h)))) — confirmed ✓

[S7] q_meta formula: clamp(q_d + 0.5 q_a, max=1) with q_d = mean(m[:2]),
  q_a = mean(m[2:]) — confirmed from code ✓

[S8] §4.5 residual injection Q3 discussion text:
  "The joint mechanism of q_meta scaling (Eq. qmeta), zero-initialised
   residual injection (Eq. hts), and sample meta-dropout..."
   — consistent with corrected §4.5 ✓

[S9] Table tab:dataset prevalence row [43.6, 25.1, 24.0, 22.4, 12.2]%
  cross-checked against actual pos_weights:
  NORM: neg/pos = 1.2930 → f = 1/(1+1.2930) = 43.6% ✓
  MI: neg/pos = 2.9776 → f = 25.1% ✓
  STTC: neg/pos = 3.1610 → f = 24.0% ✓
  CD: neg/pos = 3.4582 → f = 22.4% ✓
  HYP: neg/pos = 7.2199 → f = 12.2% ✓

[S10] §4.8 parameter budget: W_addon now described as "active in E3
  ablation mode only" — consistent with code and Group E description ✓
  Both W_res and W_addon still contribute to the 0.26M count ✓

[S11] §5.4 Group D: manuscript correctly states "10 seeds each" / n=10
  (analysis script uses SEEDS_10; 20 noaug seeds were trained but
  only 10 are analyzed — no manuscript error; noted in Pass 1) ✓

[S12] LaTeX compilation: 26 pages, 1719617 bytes, no errors,
  no undefined references ✓

=== SUMMARY OF CONFRONTATION PASSES 1–2 ===

FIXES APPLIED: 5
  [CF1] §4.5 W_sum formula: replaced with W_res-only equation
  [CF1] Fig 1 caption: corrected projection description
  [CF1] §4.8: roles of W_res and W_addon clarified
  [CF2] §3.4: pos-weights formula corrected to actual clip(neg/pos)
  [CF5] Table tab:dataset: NORM weight corrected 1.00 → 1.29

VERIFIED CORRECT (36 architectural/statistical claims):
  Architecture, training loop, augmentation, metadata encoding,
  quality score, three heads, GLU gate, loss weights, optimizer,
  seeds, groups, experimental design — all match code ✓

OBSERVATION (no fix needed):
  [CF3] 20 noaug seeds trained in Group D; only 10 used in analysis.
  Manuscript correctly reports n=10 (consistent with analysis script).

=== MANUSCRIPT STATUS AFTER CONFRONTATION PASSES 1–2 ===

All known code-manuscript discrepancies have been resolved.
The manuscript now correctly describes:
  (a) The single-projection W_res residual injection (standard mode)
  (b) W_addon as an E3-only ablation path (not standard)
  (c) The actual pos-weights formula (neg/pos, clipped) and values
  (d) The NORM weight as ≈1.29 (not 1.0) in both text and table
  (e) Dynamic per-run weight computation (not "held fixed")

===============================================================
CONFRONTATION PASS 2 COMPLETE — No new issues found
Manuscript confirmed code-consistent after 5 confrontation fixes
===============================================================
