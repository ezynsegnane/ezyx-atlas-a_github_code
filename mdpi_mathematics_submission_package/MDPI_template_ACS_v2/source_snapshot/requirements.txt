# All 250 training runs (60 Group A confirmatory + 190 across exploratory Groups B-F)
# were executed on a CPU-only machine (Intel Core i5, 8 GB RAM) using PyTorch
# 2.3.1+cpu (no CUDA/GPU). To reproduce without a GPU, install the CPU-only wheel:
#   pip install torch==2.3.1+cpu --index-url https://download.pytorch.org/whl/cpu
# Installing torch==2.3.1 without the index-url may pull the GPU wheel, which will
# also work but may introduce minor CUDA non-determinism not present in the original runs.

numpy==1.26.4
pandas==2.2.3
pyarrow==23.0.1
scikit-learn==1.3.2
scipy==1.16.3
matplotlib==3.8.4
torch==2.3.1+cpu
tqdm==4.67.3
wfdb==4.3.0
